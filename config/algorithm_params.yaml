# config/algorithm_params.yaml

yolo:
  model_path: "external_models/model_hub_n.pt"
  conf_threshold: 0.5
  iou_threshold: 0.45

priority_scoring:
  weights:
    confidence: 0.3
    maturity: 0.3
    accessibility: 0.2
    distance: 0.2

depth_estimation:
  real_diameter_mm: 80
